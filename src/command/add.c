#include <stdio.h>

#include "commands.h"

int commandAdd(int argc, const char **argv, struct configuration *config) {
  /* FILE *fd = fopen(config->worldfile, "a"); */
  /* if (!fd) { */
  /*   fprintf(stderr, "Could not open '%s', config->worldfile"); */
  /*   return 1; */
  /* } */

  

  /* /1* printf("ADD\n"); *1/ */
  /* /1* printf("Argc: %d\n", argc); *1/ */
  /* /1* for(int i=0; i<argc; i++) { *1/ */
  /*   /1* printf("argument: %s\n", argv[i]); *1/ */
  /* /1* } *1/ */

  /* fclose(fd); */
  return 0;
}
